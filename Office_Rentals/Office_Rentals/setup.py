from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='This project is done in order to show the types of rents, location, and which is the cheapest to the most expensive',
    author='oy0rzabal',
    license='MIT',
)
